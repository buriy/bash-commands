#!/bin/bash
ROOT='/var'
if [[ `ls /Projects/` ]]; then ROOT="/Projects"; fi
PROJECT=$1
if [ ! $1 ]; then
    ls -ApGH -d $ROOT/*/* | cut -d "/" -f 4;
else
    CHOICES=`ls -d $ROOT/*/$1`
    echo "Directory is now $CHOICES"
    cd $CHOICES
    ls -ApGH
fi;
